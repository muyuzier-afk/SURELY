# 开发检查清单 (CheckList)

## 阶段一：基础传输层（QUIC + TLS 1.3）

### 任务
- [x] 实现标准 QUIC 连接（使用 quic-go 库）
  - [ ] 支持 0-RTT 快速重连
  - [ ] 支持连接迁移
- [x] 定制 TLS 握手
  - [x] 伪造 ClientHello
  - [x] random 字段前 8 字节设为时间戳，其余伪随机
  - [x] cipher_suites 只包含常见套件
- [ ] 明文传输参数
  - [ ] 连接 ID 严格遵循 RFC 9000 规范
  - [ ] 包号从 0 递增
  - [ ] 版本号固定为 0x00000001

### 验收标准
- [x] QUIC 连接能正常建立
- [ ] TLS 握手符合定制要求
- [ ] 传输参数符合 RFC 9000 规范

## 阶段二：HTTP/3 与 MASQUE 框架

### 任务
- [x] 实现 HTTP/3 会话
  - [x] 在 QUIC 上叠加 HTTP/3
  - [x] 支持请求/响应流
- [x] 集成 MASQUE 核心
  - [x] 实现 CONNECT 方法
  - [ ] 实现 DATAGRAM 帧
  - [x] 支持 UDP 代理
  - [x] 支持任意数据流复用
- [ ] 控制帧伪装
  - [ ] 发送 SETTINGS 帧
  - [ ] 发送 MAX_DATA 帧
  - [ ] 发送 MAX_STREAMS 帧

### 验收标准
- [x] HTTP/3 会话能正常建立
- [x] MASQUE 核心功能正常
- [ ] 控制帧伪装符合要求

## 阶段三：对抗检测的填充与整形模块

### 任务
- [x] 动态包长填充
  - [x] 收集普通 HTTPS/HTTP/3 会话的包长分布
  - [x] 按概率对隧道包进行填充
- [x] 时序整形
  - [x] 模拟真实应用的随机抖动（5-50ms）
  - [x] 避免完美周期性发包
- [x] 低熵背景噪声
  - [x] 发送模拟 Keep-Alive 数据包
  - [x] 发送模拟视频黑屏流的低熵数据包

### 验收标准
- [ ] 包长分布符合真实流量特征
- [ ] 时序抖动符合要求
- [ ] 低熵背景噪声正常发送

## 阶段四：应用层行为模拟

### 任务
- [x] 模拟真实请求
  - [x] 定期发送伪装 HTTP/3 请求（如 GET / 或 GET /health）
  - [x] 附带常见 User-Agent
- [ ] 动态证书加载
  - [ ] 服务端配置多域名证书
  - [ ] 根据连接指向返回不同证书
- [x] 错误与重试模拟
  - [x] 按概率（<1%）返回标准 HTTP 错误码（如 404、503）
  - [x] 模拟浏览器重试

### 验收标准
- [x] 模拟请求符合真实浏览器行为
- [ ] 动态证书加载正常
- [x] 错误与重试模拟符合要求

## 阶段五：审查对抗验证与迭代

### 任务
- [x] 构建测试环境
  - [x] 本地搭建模拟审查系统（基于 nDPI 或自研熵检测脚本）
- [x] 特征迭代
  - [x] 对比真实 TLS 1.3+HTTP/3 流量的熵分布
  - [x] 对比包长序列
  - [x] 对比到达间隔
  - [x] 通过 KL 散度量化差异并优化填充策略
- [ ] 现场测试
  - [ ] 在已知部署了深度审查的网络环境中进行小规模盲测

### 验收标准
- [x] 模拟审查系统正常运行
- [x] 特征优化符合要求
- [ ] 现场测试成功率高于 90%

## 关键技术实现验证

### 首包构造（对抗核心）
- [ ] QUIC 长包头构造正确
  - [ ] 连接 ID 使用可预测模式
  - [ ] 包号从 0 递增
  - [ ] 版本号固定为 0x00000001
- [ ] TLS ClientHello 构造正确
  - [ ] 严格按照规范构造
  - [ ] 禁止全字段随机化

### 填充与噪声生成
- [ ] 包长填充实现
  - [ ] 维护目标包长分布表
  - [ ] 按概率选择填充长度
- [ ] 多级填充实现
  - [ ] QUIC 层小范围填充（±32 字节）
  - [ ] MASQUE 层精确填充
- [ ] 噪声数据生成
  - [ ] 使用真实但无害的数据
  - [ ] 避免使用纯随机字节

### 状态机与降级机制
- [ ] 探测模式实现
  - [ ] 发送试探性伪装包
  - [ ] 自动降级为更保守的伪装策略
- [ ] 会话复用实现
  - [ ] 启用 TLS 会话票据
  - [ ] 0-RTT 重连时使用上次的伪装参数

## 测试与验收

### 单元测试
- [ ] 各模块独立测试
  - [ ] QUIC 连接测试
  - [ ] TLS 握手测试
  - [ ] 包长填充测试
  - [ ] 噪声生成测试
- [ ] 熵值测试
  - [ ] 单个数据包的滑动窗口熵值不高于 0.85

### 集成测试
- [ ] 模拟审查环境测试
  - [ ] 连续运行 24 小时，阻断率低于 5%
- [ ] 流量对比测试
  - [ ] 包长分布的 KL 散度小于 0.1
  - [ ] 到达间隔的 JS 散度小于 0.15

### 现场测试
- [ ] 在至少 3 个已知部署审查的网络中测试
  - [ ] 每个网络完成 100 次连接尝试
  - [ ] 成功率高于 90%

## 风险与应对

### 风险：MASQUE 本身被深度审查系统针对性识别
- [ ] 准备降级方案
  - [ ] 将外层从 MASQUE 切换为普通 HTTP/3 CONNECT 代理
  - [ ] 增加一层 "TLS over TLS" 封装

### 风险：Go 运行时环境的指纹特征被识别
- [ ] 使用 utls 库模拟主流浏览器的 TLS 握手栈
- [ ] User-Agent 从真实浏览器列表随机选择

### 风险：填充算法增加过多开销
- [ ] 对延迟敏感的数据流使用最小填充
- [ ] 仅对首包和空闲时段进行密集整形

## 文档与维护

- [ ] 保留每次对抗测试的 pcap 文件和熵值分析报告
- [ ] 维护 "特征指纹库"，记录不同审查系统的行为模式
- [ ] 定期更新伪装策略，与主流浏览器的行为保持同步

## 开发进度跟踪

### 阶段一：基础传输层
- [ ] 开始时间：______
- [ ] 完成时间：______
- [ ] 状态：______（进行中/已完成/未开始）

### 阶段二：HTTP/3 与 MASQUE 框架
- [ ] 开始时间：______
- [ ] 完成时间：______
- [ ] 状态：______（进行中/已完成/未开始）

### 阶段三：对抗检测的填充与整形模块
- [ ] 开始时间：______
- [ ] 完成时间：______
- [ ] 状态：______（进行中/已完成/未开始）

### 阶段四：应用层行为模拟
- [ ] 开始时间：______
- [ ] 完成时间：______
- [ ] 状态：______（进行中/已完成/未开始）

### 阶段五：审查对抗验证与迭代
- [ ] 开始时间：______
- [ ] 完成时间：______
- [ ] 状态：______（进行中/已完成/未开始）

## 总体状态
- [ ] 项目启动：______
- [ ] 项目完成：______
- [ ] 总体状态：______（进行中/已完成/未开始）
